import 'package:flutter/material.dart';

class ShoppingCartPanel extends StatelessWidget {
  const ShoppingCartPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Varukorg'));
  }
}